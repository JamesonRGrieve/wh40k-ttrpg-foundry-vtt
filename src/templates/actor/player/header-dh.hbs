{{!-- Dark Heresy 2e Character Sheet Header — sidebar layout for PC + NPC.
     Per-system entry file (referenced by the `headerFile` mapping in
     `game-system-sheets.ts`). Delegates to the shared `header-sidebar`
     partial, which is the single source of truth for the sidebar header
     body shared with RT (and the BC/DW/OW/DH1/IM systems that also map
     their `headerFile` here). --}}

{{> systems/wh40k-rpg/templates/actor/player/header-sidebar.hbs}}
