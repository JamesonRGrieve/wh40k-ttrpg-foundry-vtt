{{!-- Vehicle Components Tab --}}
<section class="tab {{#if tab.active}}active{{/if}} {{tab.cssClass}}" data-tab="{{tab.id}}" data-group="{{tab.group}}">
    <div class="wh40k-vehicle-components">
        
        {{!-- Components/Upgrades List --}}
        <div class="wh40k-vehicle-panel wh40k-vehicle-components-list">
            <h3 class="wh40k-vehicle-panel-title">
                <i class="fas fa-cog"></i>
                Components & Upgrades
            </h3>
            <div class="wh40k-vehicle-items-list">
                {{#each components}}
                <div class="wh40k-vehicle-component-item" data-item-id="{{this._id}}">
                    <div class="wh40k-vehicle-component-name">
                        <input type="checkbox" 
                               data-action="toggleComponentActive" 
                               data-item-id="{{this._id}}"
                               {{#if this.system.active}}checked{{/if}} />
                        {{this.name}}
                    </div>
                    {{#if this.system.description}}
                    <div class="wh40k-vehicle-component-desc">
                        {{{this.system.description}}}
                    </div>
                    {{/if}}
                </div>
                {{/each}}
                
                {{#each vehicleUpgrades}}
                <div class="wh40k-vehicle-component-item" data-item-id="{{this._id}}">
                    <div class="wh40k-vehicle-component-name">
                        {{this.name}}
                    </div>
                    {{#if this.system.description}}
                    <div class="wh40k-vehicle-component-desc">
                        {{{this.system.description}}}
                    </div>
                    {{/if}}
                </div>
                {{/each}}
                
                {{#unless (or components.length vehicleUpgrades.length)}}
                <div class="wh40k-vehicle-empty">No components or upgrades</div>
                {{/unless}}
            </div>
        </div>
        
    </div>
</section>
