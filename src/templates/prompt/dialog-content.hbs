{{!--
    Content PART for DialogWH40K (src/module/applications/api/dialog.ts).
    Renders the caller-supplied content HTML. Buttons are emitted by the
    separate footer PART (templates/generic/form-footer.hbs) from the
    `buttons` context prepared in _prepareContext.
--}}
<div class="dialog-content tw-flex tw-flex-col tw-gap-2">
    {{{content}}}
</div>
