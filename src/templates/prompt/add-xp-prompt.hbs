{{!-- Add XP Dialog Template --}}
<div class="wh40k-dialog-content">
    
    {{!-- Current XP Display --}}
    <div class="wh40k-dialog-section">
        <div class="wh40k-xp-current-display">
            <span class="wh40k-xp-label">Current Total XP:</span>
            <span class="wh40k-xp-value">{{currentTotal}}</span>
        </div>
    </div>

    {{!-- XP Input --}}
    <div class="wh40k-dialog-section">
        <label class="wh40k-dialog-label">
            <span class="wh40k-dialog-label-text">XP to Add/Subtract:</span>
            <input type="number" 
                   name="xpAmount" 
                   value="{{xpAmount}}" 
                   data-dtype="Number"
                   class="wh40k-dialog-input"
                   placeholder="0"
                   step="1" />
        </label>
        <p class="wh40k-dialog-hint">
            <i class="fas fa-info-circle"></i>
            Enter a positive number to add XP, or negative to subtract.
        </p>
    </div>

    {{!-- Preview New Total --}}
    {{#if (ne xpAmount 0)}}
    <div class="wh40k-dialog-section wh40k-xp-preview">
        <div class="wh40k-xp-preview-box {{#if isAddition}}wh40k-xp-preview--positive{{else}}wh40k-xp-preview--negative{{/if}}">
            <div class="wh40k-xp-preview-change">
                <i class="fas {{#if isAddition}}fa-arrow-up{{else}}fa-arrow-down{{/if}}"></i>
                {{#if isAddition}}+{{else}}-{{/if}}{{absAmount}} XP
            </div>
            <div class="wh40k-xp-preview-result">
                <span class="wh40k-xp-preview-label">New Total:</span>
                <span class="wh40k-xp-preview-value">{{newTotal}}</span>
            </div>
        </div>
    </div>
    {{/if}}

    {{!-- Action Buttons --}}
    <div class="wh40k-dialog-buttons">
        <button type="button" 
                class="wh40k-dialog-button wh40k-dialog-button--primary"
                data-action="apply"
                {{#if (eq xpAmount 0)}}disabled{{/if}}>
            <i class="fas fa-check"></i> Apply
        </button>
        <button type="button" 
                class="wh40k-dialog-button wh40k-dialog-button--secondary"
                data-action="cancel">
            <i class="fas fa-times"></i> Cancel
        </button>
    </div>

</div>
