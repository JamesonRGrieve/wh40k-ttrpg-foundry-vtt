{{!-- 
  Rogue Trader Trait Item Sheet - Modern Tabbed Redesign
  Clean trait configuration with tabbed interface
--}}
<div class="wh40k-trait-sheet">
  <form autocomplete="off">
    
    {{!-- ═══════════════════════════════════════════════════════════════════
         HEADER: Compact trait identity with inline badges
    ═══════════════════════════════════════════════════════════════════ --}}
    <header class="wh40k-trait-header">
      <div class="wh40k-trait-header__image" data-action="editImage" data-edit="img">
        <img src="{{item.img}}" alt="{{item.name}}" />
        <div class="wh40k-trait-header__image-overlay">
          <i class="fas fa-edit"></i>
        </div>
      </div>
      
      <div class="wh40k-trait-header__info">
        <input type="text" class="wh40k-trait-header__name" name="name" value="{{item.name}}" placeholder="Trait Name" />
        
        <div class="wh40k-trait-header__meta">
          {{!-- Category Badge --}}
          <span class="wh40k-trait-badge wh40k-trait-badge--category" title="{{item.system.categoryLabel}}">
            <i class="fas fa-shield-alt"></i>
            {{item.system.categoryLabel}}
          </span>
          
          {{!-- Level Badge (if applicable) --}}
          {{#if item.system.hasLevel}}
          <span class="wh40k-trait-badge wh40k-trait-badge--level" title="Trait Level/Rating">
            <i class="fas fa-layer-group"></i>
            Level {{item.system.level}}
          </span>
          {{/if}}
          
          {{!-- Variable Indicator --}}
          {{#if item.system.isVariable}}
          <span class="wh40k-trait-badge wh40k-trait-badge--variable" title="Variable Trait (X)">
            <i class="fas fa-question"></i>
            Variable
          </span>
          {{/if}}
        </div>
      </div>
    </header>

    {{!-- ═══════════════════════════════════════════════════════════════════
         STATS BAR: Key trait info at a glance
    ═══════════════════════════════════════════════════════════════════ --}}
    <div class="wh40k-trait-stats">
      {{!-- Category --}}
      <div class="wh40k-trait-stat wh40k-trait-stat--category">
        <div class="wh40k-trait-stat__icon">
          <i class="fas fa-shield-alt"></i>
        </div>
        <div class="wh40k-trait-stat__content">
          <span class="wh40k-trait-stat__label">Category</span>
          <span class="wh40k-trait-stat__value">{{item.system.categoryLabel}}</span>
        </div>
      </div>
      
      {{!-- Level/Rating --}}
      {{#if (gt item.system.level 0)}}
      <div class="wh40k-trait-stat wh40k-trait-stat--level">
        <div class="wh40k-trait-stat__icon">
          <i class="fas fa-layer-group"></i>
        </div>
        <div class="wh40k-trait-stat__content">
          <span class="wh40k-trait-stat__label">Level</span>
          <span class="wh40k-trait-stat__value">{{item.system.level}}</span>
        </div>
      </div>
      {{/if}}
      
      {{!-- Variable indicator --}}
      {{#if item.system.isVariable}}
      <div class="wh40k-trait-stat">
        <div class="wh40k-trait-stat__icon">
          <i class="fas fa-question"></i>
        </div>
        <div class="wh40k-trait-stat__content">
          <span class="wh40k-trait-stat__label">Type</span>
          <span class="wh40k-trait-stat__value">Variable (X)</span>
        </div>
      </div>
      {{/if}}
    </div>

    {{!-- ═══════════════════════════════════════════════════════════════════
         TAB NAVIGATION
    ═══════════════════════════════════════════════════════════════════ --}}
    <nav class="wh40k-tabs wh40k-tabs--trait wh40k-trait-tabs" data-group="primary">
      <button type="button" class="wh40k-tab wh40k-trait-tab active" data-tab="properties" data-group="primary">
        <i class="fas fa-cog"></i>
        <span>Properties</span>
      </button>
      <button type="button" class="wh40k-tab wh40k-trait-tab" data-tab="effects" data-group="primary">
        <i class="fas fa-magic"></i>
        <span>Effects</span>
      </button>
      <button type="button" class="wh40k-tab wh40k-trait-tab" data-tab="description" data-group="primary">
        <i class="fas fa-book"></i>
        <span>Description</span>
      </button>
    </nav>

    {{!-- ═══════════════════════════════════════════════════════════════════
         TAB CONTENT
    ═══════════════════════════════════════════════════════════════════ --}}
    <section class="wh40k-trait-content wh40k-tab-content wh40k-tab-content--trait">
      
      {{!-- PROPERTIES TAB --}}
      <div class="wh40k-trait-panel tab active" data-tab="properties" data-group="primary">
        <div class="wh40k-trait-section">
          <h3 class="wh40k-section-title">
            <i class="fas fa-info-circle"></i> Basic Properties
          </h3>
          
          <div class="wh40k-properties-grid">
            {{!-- Category --}}
            <div class="wh40k-property">
              <label class="wh40k-property-label">Category</label>
              <select name="system.category" class="wh40k-property-input">
                {{#select item.system.category}}
                <option value="general">General</option>
                <option value="creature">Creature</option>
                <option value="character">Character</option>
                <option value="elite">Elite</option>
                <option value="unique">Unique</option>
                <option value="origin">Origin Path</option>
                <option value="physical">Physical</option>
                <option value="mental">Mental</option>
                <option value="social">Social</option>
                {{/select}}
              </select>
            </div>
            
            {{!-- Level/Rating --}}
            <div class="wh40k-property">
              <label class="wh40k-property-label">Level / Rating <span class="wh40k-field-hint">(0 = none)</span></label>
              <input type="number" name="system.level" value="{{item.system.level}}" min="0" class="wh40k-property-input" placeholder="0" />
            </div>
          </div>
        </div>
        
        {{!-- Requirements --}}
        <div class="wh40k-trait-section">
          <h3 class="wh40k-section-title">
            <i class="fas fa-lock"></i> Requirements
          </h3>
          
          <div class="wh40k-property">
            <label class="wh40k-property-label">Prerequisites <span class="wh40k-field-hint">(to acquire this trait)</span></label>
            <input type="text" name="system.requirements" value="{{item.system.requirements}}" class="wh40k-property-input" placeholder="e.g., Mutant origin, Corruption 50+" />
          </div>
          
          {{#if item.system.requirements}}
          <div class="wh40k-requirements-display" style="margin-top: 0.75rem;">
            <div class="wh40k-requirement-tag">
              <i class="fas fa-exclamation-triangle"></i>
              {{item.system.requirements}}
            </div>
          </div>
          {{/if}}
        </div>
      </div>
      
      {{!-- EFFECTS TAB --}}
      <div class="wh40k-trait-panel tab" data-tab="effects" data-group="primary">
        {{!-- Benefit/Effect Description --}}
        <div class="wh40k-trait-section">
          <h3 class="wh40k-section-title">
            <i class="fas fa-magic"></i> Benefit
          </h3>
          
          <p class="wh40k-field-hint" style="margin-bottom: 0.75rem;">
            <i class="fas fa-info-circle"></i> Describe the mechanical effect or benefit of this trait.
          </p>
          
          <div class="wh40k-editor-container">
            {{#if inEditMode}}
            {{editor system.benefit target="system.benefit" button=true editable=editable engine="prosemirror"}}
            {{else}}
            <div class="wh40k-description-content">{{{system.benefit}}}</div>
            {{/if}}
          </div>
        </div>
        
        {{!-- Modifiers Display --}}
        {{#if (or item.system.modifiers.characteristics item.system.modifiers.skills item.system.modifiers.combat item.system.modifiers.wounds item.system.modifiers.movement)}}
        <div class="wh40k-trait-section">
          <h3 class="wh40k-section-title">
            <i class="fas fa-chart-line"></i> Modifiers
          </h3>
          
          <div class="wh40k-modifiers-display">
            {{#if item.system.modifiers.characteristics}}
            <div class="wh40k-modifier-group">
              <h4 class="wh40k-modifier-group-title">Characteristics</h4>
              <div class="wh40k-modifier-list">
                {{#each item.system.modifiers.characteristics}}
                <div class="wh40k-modifier-item">
                  <span class="wh40k-modifier-name">{{capitalize @key}}</span>
                  <span class="wh40k-modifier-value {{#if (gt this 0)}}positive{{else}}negative{{/if}}">
                    {{signedNumber this}}
                  </span>
                </div>
                {{/each}}
              </div>
            </div>
            {{/if}}
            
            {{#if item.system.modifiers.skills}}
            <div class="wh40k-modifier-group">
              <h4 class="wh40k-modifier-group-title">Skills</h4>
              <div class="wh40k-modifier-list">
                {{#each item.system.modifiers.skills}}
                <div class="wh40k-modifier-item">
                  <span class="wh40k-modifier-name">{{capitalize @key}}</span>
                  <span class="wh40k-modifier-value {{#if (gt this 0)}}positive{{else}}negative{{/if}}">
                    {{signedNumber this}}
                  </span>
                </div>
                {{/each}}
              </div>
            </div>
            {{/if}}
            
            {{#if item.system.modifiers.combat}}
            <div class="wh40k-modifier-group">
              <h4 class="wh40k-modifier-group-title">Combat</h4>
              <div class="wh40k-modifier-list">
                {{#each item.system.modifiers.combat}}
                <div class="wh40k-modifier-item">
                  <span class="wh40k-modifier-name">{{capitalize @key}}</span>
                  <span class="wh40k-modifier-value {{#if (gt this 0)}}positive{{else}}negative{{/if}}">
                    {{signedNumber this}}
                  </span>
                </div>
                {{/each}}
              </div>
            </div>
            {{/if}}
            
            {{#if item.system.modifiers.wounds}}
            <div class="wh40k-modifier-group">
              <h4 class="wh40k-modifier-group-title">Other</h4>
              <div class="wh40k-modifier-list">
                <div class="wh40k-modifier-item">
                  <span class="wh40k-modifier-name">Wounds</span>
                  <span class="wh40k-modifier-value {{#if (gt item.system.modifiers.wounds 0)}}positive{{else}}negative{{/if}}">
                    {{signedNumber item.system.modifiers.wounds}}
                  </span>
                </div>
              </div>
            </div>
            {{/if}}
            
            {{#if item.system.modifiers.movement}}
            <div class="wh40k-modifier-group">
              <h4 class="wh40k-modifier-group-title">Movement</h4>
              <div class="wh40k-modifier-list">
                <div class="wh40k-modifier-item">
                  <span class="wh40k-modifier-name">Movement Bonus</span>
                  <span class="wh40k-modifier-value {{#if (gt item.system.modifiers.movement 0)}}positive{{else}}negative{{/if}}">
                    {{signedNumber item.system.modifiers.movement}}
                  </span>
                </div>
              </div>
            </div>
            {{/if}}
          </div>
          
          <p class="wh40k-field-hint" style="margin-top: 1rem;">
            <i class="fas fa-info-circle"></i> Modifiers are configured via Active Effects. This panel is read-only.
          </p>
        </div>
        {{else}}
        <div class="wh40k-trait-section">
          <div class="wh40k-empty-state">
            <i class="fas fa-chart-line"></i>
            <p>No modifiers defined</p>
            <p class="wh40k-field-hint">Use Active Effects tab to add stat bonuses</p>
          </div>
        </div>
        {{/if}}
      </div>
      
      {{!-- DESCRIPTION TAB --}}
      <div class="wh40k-trait-panel tab" data-tab="description" data-group="primary">
        {{!-- Description --}}
        <div class="wh40k-trait-section">
          <h3 class="wh40k-section-title">
            <i class="fas fa-book"></i> Description
          </h3>
          
          <p class="wh40k-field-hint" style="margin-bottom: 0.75rem;">
            <i class="fas fa-info-circle"></i> Additional lore, flavor text, or detailed explanation.
          </p>
          
          <div class="wh40k-editor-container">
            {{#if inEditMode}}
            {{editor system.description.value target="system.description.value" button=true editable=editable engine="prosemirror"}}
            {{else}}
            <div class="wh40k-description-content">{{{system.description.value}}}</div>
            {{/if}}
          </div>
        </div>
        
        {{!-- Notes --}}
        <div class="wh40k-trait-section">
          <h3 class="wh40k-section-title">
            <i class="fas fa-sticky-note"></i> Notes
          </h3>
          
          <div class="wh40k-property">
            <textarea name="system.notes" rows="4" class="wh40k-property-textarea" placeholder="GM notes, clarifications, house rules, etc.">{{item.system.notes}}</textarea>
          </div>
        </div>
        
        {{!-- Source --}}
        <div class="wh40k-trait-section">
          <h3 class="wh40k-section-title">
            <i class="fas fa-book-bookmark"></i> Source
          </h3>
          
          <div class="wh40k-property">
            <input type="text" name="system.source" value="{{item.system.source}}" class="wh40k-property-input" placeholder="Core Rulebook, pg. XX" />
          </div>
        </div>
      </div>
      
    </section>{{!-- .wh40k-trait-content --}}
  </form>
</div>{{!-- .wh40k-trait-sheet --}}
