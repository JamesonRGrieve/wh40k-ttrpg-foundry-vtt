{{!-- Modifiers Summary Bar
     Compact inline display of active modifiers for quick reference
     Shows at top of talent/trait/condition sheets when hasModifiers is true
--}}
<div class="wh40k-modifiers-summary">
  <div class="wh40k-modifiers-summary__header">
    <i class="fas fa-sliders-h"></i>
    <span>Active Modifiers</span>
  </div>
  
  <div class="wh40k-modifiers-summary__list">
    {{#each system.modifiersList}}
    <span class="wh40k-modifier-badge wh40k-modifier-badge--{{type}} {{#if (gt value 0)}}wh40k-modifier-badge--positive{{else}}wh40k-modifier-badge--negative{{/if}}" 
          title="{{label}}">
      <span class="wh40k-modifier-badge__label">{{label}}</span>
      <span class="wh40k-modifier-badge__value">{{signedNumber value}}</span>
    </span>
    {{/each}}
    
    {{!-- Show situational indicator if present --}}
    {{#if system.hasSituationalModifiers}}
    <span class="wh40k-modifier-badge wh40k-modifier-badge--situational" 
          title="Has situational modifiers">
      <i class="fas fa-exclamation-triangle"></i>
      <span>Situational</span>
    </span>
    {{/if}}
    
    {{#unless system.modifiersList.length}}
    <span class="wh40k-modifiers-summary__empty">
      <i class="fas fa-info-circle"></i>
      No active modifiers
    </span>
    {{/unless}}
  </div>
</div>
