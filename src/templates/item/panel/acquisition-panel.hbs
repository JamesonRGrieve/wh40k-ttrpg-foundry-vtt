{{!-- Acquisition Panel
     Weight, Availability, and Craftsmanship configuration for physical items
     Uses PhysicalItemTemplate fields
--}}
<div class="wh40k-acquisition-panel">
  <header class="wh40k-panel-header">
    <i class="fas fa-coins"></i>
    <span>{{localize "WH40K.Acquisition"}}</span>
  </header>
  
  <div class="wh40k-panel-body">
    <div class="wh40k-acquisition-grid">
      
      {{!-- Weight --}}
      <div class="wh40k-field">
        <label class="wh40k-field__label">
          <i class="fas fa-weight-hanging"></i>
          {{localize "WH40K.Weight"}}
        </label>
        <div class="wh40k-field__input-group">
          <input class="wh40k-field__input wh40k-field__input--numeric" 
                 name="system.weight" 
                 type="number" 
                 value="{{source.weight}}" 
                 step="0.1" 
                 min="0" 
                 placeholder="0" />
          <span class="wh40k-field__unit">kg</span>
        </div>
      </div>
      
      {{!-- Availability --}}
      <div class="wh40k-field">
        <label class="wh40k-field__label">
          <i class="fas fa-store"></i>
          {{localize "WH40K.Availability"}}
        </label>
        <select class="wh40k-field__input wh40k-field__select" name="system.availability">
          <option value="">{{localize "WH40K.Availability.None"}}</option>
          {{#each config.availabilities}}
          <option value="{{@key}}" {{selected (eq @key ../system.availability)}}>
            {{label}} ({{signedNumber modifier}})
          </option>
          {{/each}}
        </select>
      </div>
      
      {{!-- Craftsmanship --}}
      <div class="wh40k-field">
        <label class="wh40k-field__label">
          <i class="fas fa-hammer"></i>
          {{localize "WH40K.Craftsmanship"}}
        </label>
        <select class="wh40k-field__input wh40k-field__select" name="system.craftsmanship">
          {{#each config.craftsmanships}}
          <option value="{{@key}}" {{selected (eq @key ../system.craftsmanship)}}>
            {{label}} ({{signedNumber modifier}})
          </option>
          {{/each}}
        </select>
      </div>
      
    </div>
    
    {{!-- Acquisition Info Display --}}
    {{#if system.acquisitionInfo}}
    <div class="wh40k-acquisition-summary">
      <i class="fas fa-info-circle"></i>
      <span>{{system.acquisitionInfo}}</span>
    </div>
    {{/if}}
  </div>
</div>
