<div class="rt-item-sheet rt-compact">
    <form autocomplete="off">
        <!-- Compact Header -->
        <header class="rt-item-header rt-item-header--compact">
            <div class="rt-item-image rt-item-image--sm" data-edit="img">
                <img src="{{item.img}}" alt="{{item.name}}" />
            </div>
            <div class="rt-item-title">
                <input type="text" class="rt-item-title__name" name="name" value="{{item.name}}" placeholder="Trait Name" />
                <div class="rt-item-meta">
                    <span class="rt-meta-badge rt-meta-badge--type"><i class="fas fa-shield-alt"></i> Trait</span>
                    <input type="text" class="rt-meta-text" name="system.category" value="{{item.system.category}}" placeholder="Category" />
                    <div class="rt-meta-input">
                        <span>Lvl</span>
                        <input type="number" name="system.level" value="{{item.system.level}}" min="0" placeholder="-" />
                    </div>
                </div>
            </div>
        </header>

        <!-- Compact Body -->
        <div class="rt-item-body rt-item-body--compact">
            <!-- Requirements -->
            <div class="rt-inline-field">
                <label><i class="fas fa-lock"></i> Requirements</label>
                <input type="text" name="system.requirements" value="{{item.system.requirements}}" placeholder="None" />
            </div>

            <!-- Effect -->
            <div class="rt-prose-field">
                <label>Effect</label>
                <div class="rt-prose" data-edit="system.effect">{{{item.system.effect}}}{{{item.system.effects}}}</div>
            </div>

            <!-- Footer -->
            <div class="rt-item-footer">
                <input type="text" class="rt-source-input" name="system.source" value="{{#if item.system.source}}{{item.system.source}}{{else}}Core Rulebook{{/if}}" placeholder="Core Rulebook" />
            </div>
        </div>
    </form>
</div>
