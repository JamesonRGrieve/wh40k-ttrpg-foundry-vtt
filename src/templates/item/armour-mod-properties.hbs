{{!-- Armour Modification Properties Tab --}}
<div class="tw-flex-1 tw-overflow-y-auto tw-overflow-x-hidden tw-min-h-0 tw-p-4" data-tab="properties">
    <div class="tw-grid tw-grid-cols-2 tw-gap-6">
        {{!-- Added Properties Column --}}
        <div class="tw-rounded-lg tw-overflow-hidden tw-border tw-border-[rgba(212,197,166,0.15)]" style="background: rgba(255,255,255,0.02)">
            <header class="tw-px-5 tw-py-4 tw-border-b-2 tw-border-gold bc:tw-border-crimson-light dh1e:tw-border-gold-raw-l5 dh2e:tw-border-gold-raw dw:tw-border-accent-combat ow:tw-border-brass-l20 rt:tw-border-gold im:tw-border-failure" style="background: linear-gradient(135deg,rgba(46,204,113,0.15) 0%,rgba(46,204,113,0.05) 100%)">
                <h3 class="tw-flex tw-items-center tw-gap-2.5 tw-text-base tw-font-bold tw-m-0" style="color: rgba(255,255,255,0.95)">
                    <i class="fas fa-plus-circle tw-text-[var(--wh40k-accent-green-bright)]"></i>
                    {{localize "WH40K.Modification.AddedProperties"}}
                </h3>
            </header>

            <div class="tw-p-4 tw-min-h-[200px]">
                {{#each addedPropertiesArray}}
                    <div class="tw-flex tw-items-start tw-gap-3 tw-px-4 tw-py-3.5 tw-mb-2 tw-rounded-md tw-border tw-border-[rgba(212,197,166,0.2)] tw-border-l-[3px] tw-border-l-[var(--wh40k-accent-green-bright)]" style="background: rgba(255,255,255,0.04)">
                        <div class="tw-flex-1">
                            <span class="tw-font-semibold tw-text-sm" style="color: rgba(255,255,255,0.9)">{{label}}</span>
                            {{#if description}}
                                <span class="tw-block tw-text-xs tw-mt-1" style="color: rgba(255,255,255,0.5)">{{description}}</span>
                            {{/if}}
                        </div>
                        <button type="button"
                                class="tw-flex tw-items-center tw-justify-center tw-w-6 tw-h-6 tw-rounded-full tw-border-0 tw-cursor-pointer tw-text-xs tw-transition-all tw-shrink-0 hover:tw-bg-[rgba(239,68,68,0.2)] hover:tw-text-[#fca5a5]"
                                style="background: rgba(255,255,255,0.05); color: rgba(255,255,255,0.4)"
                                data-action="removeProperty"
                                data-property="{{key}}"
                                data-list="added"
                                data-tooltip="Remove property"
                                {{disabled (not ../isEditable)}}>
                            <i class="fas fa-times"></i>
                        </button>
                    </div>
                {{else}}
                    <p class="tw-text-center tw-p-4 tw-italic" style="color: rgba(255,255,255,0.4)">
                        <i class="fas fa-info-circle tw-mr-2"></i>
                        No properties added
                    </p>
                {{/each}}
            </div>

            {{#if isEditable}}
                <div class="tw-flex tw-flex-col tw-gap-2 tw-p-4 tw-border-t tw-border-[rgba(212,197,166,0.15)]" style="background: rgba(0,0,0,0.15)">
                    <label class="tw-text-xs tw-font-semibold tw-uppercase tw-text-[var(--wh40k-text-muted)]">Add Property</label>
                    <select class="tw-w-full tw-px-2 tw-py-1 tw-border tw-border-[var(--wh40k-border-color)] tw-rounded-sm tw-bg-[var(--wh40k-bg-primary)]" data-add-list="added">
                        <option value="">-- Select Property --</option>
                        {{#each availablePropertiesArray}}
                            <option value="{{key}}">{{label}}</option>
                        {{/each}}
                    </select>
                    <button type="button"
                            class="tw-w-full tw-inline-flex tw-items-center tw-justify-center tw-gap-2 tw-px-3 tw-py-3 tw-font-semibold tw-text-sm tw-uppercase tw-tracking-wide tw-rounded-md tw-cursor-pointer tw-transition-all tw-border tw-border-[var(--wh40k-gold-dark)] tw-text-[var(--wh40k-color-gold)] hover:tw-translate-y-[-1px]"
                            style="background: linear-gradient(135deg,rgba(212,175,55,0.2) 0%,rgba(212,175,55,0.1) 100%)"
                            data-action="addProperty"
                            data-property=""
                            data-list="added">
                        <i class="fas fa-plus"></i>
                        Add
                    </button>
                </div>
            {{/if}}
        </div>

        {{!-- Removed Properties Column --}}
        <div class="tw-rounded-lg tw-overflow-hidden tw-border tw-border-[rgba(212,197,166,0.15)]" style="background: rgba(255,255,255,0.02)">
            <header class="tw-px-5 tw-py-4 tw-border-b-2 tw-border-[rgba(231,76,60,0.3)]" style="background: linear-gradient(135deg,rgba(231,76,60,0.15) 0%,rgba(231,76,60,0.05) 100%)">
                <h3 class="tw-flex tw-items-center tw-gap-2.5 tw-text-base tw-font-bold tw-m-0" style="color: rgba(255,255,255,0.95)">
                    <i class="fas fa-minus-circle tw-text-[var(--wh40k-accent-red)]"></i>
                    {{localize "WH40K.Modification.RemovedProperties"}}
                </h3>
            </header>

            <div class="tw-p-4 tw-min-h-[200px]">
                {{#each removedPropertiesArray}}
                    <div class="tw-flex tw-items-start tw-gap-3 tw-px-4 tw-py-3.5 tw-mb-2 tw-rounded-md tw-border tw-border-[rgba(212,197,166,0.2)] tw-border-l-[3px] tw-border-l-[var(--wh40k-accent-red)]" style="background: rgba(255,255,255,0.04)">
                        <div class="tw-flex-1">
                            <span class="tw-font-semibold tw-text-sm" style="color: rgba(255,255,255,0.9)">{{label}}</span>
                            {{#if description}}
                                <span class="tw-block tw-text-xs tw-mt-1" style="color: rgba(255,255,255,0.5)">{{description}}</span>
                            {{/if}}
                        </div>
                        <button type="button"
                                class="tw-flex tw-items-center tw-justify-center tw-w-6 tw-h-6 tw-rounded-full tw-border-0 tw-cursor-pointer tw-text-xs tw-transition-all tw-shrink-0 hover:tw-bg-[rgba(239,68,68,0.2)] hover:tw-text-[#fca5a5]"
                                style="background: rgba(255,255,255,0.05); color: rgba(255,255,255,0.4)"
                                data-action="removeProperty"
                                data-property="{{key}}"
                                data-list="removed"
                                data-tooltip="Remove property"
                                {{disabled (not ../isEditable)}}>
                            <i class="fas fa-times"></i>
                        </button>
                    </div>
                {{else}}
                    <p class="tw-text-center tw-p-4 tw-italic" style="color: rgba(255,255,255,0.4)">
                        <i class="fas fa-info-circle tw-mr-2"></i>
                        No properties removed
                    </p>
                {{/each}}
            </div>

            {{#if isEditable}}
                <div class="tw-flex tw-flex-col tw-gap-2 tw-p-4 tw-border-t tw-border-[rgba(212,197,166,0.15)]" style="background: rgba(0,0,0,0.15)">
                    <label class="tw-text-xs tw-font-semibold tw-uppercase tw-text-[var(--wh40k-text-muted)]">Remove Property</label>
                    <select class="tw-w-full tw-px-2 tw-py-1 tw-border tw-border-[var(--wh40k-border-color)] tw-rounded-sm tw-bg-[var(--wh40k-bg-primary)]" data-add-list="removed">
                        <option value="">-- Select Property --</option>
                        {{#each availablePropertiesArray}}
                            <option value="{{key}}">{{label}}</option>
                        {{/each}}
                    </select>
                    <button type="button"
                            class="tw-w-full tw-inline-flex tw-items-center tw-justify-center tw-gap-2 tw-px-3 tw-py-3 tw-font-semibold tw-text-sm tw-uppercase tw-tracking-wide tw-rounded-md tw-cursor-pointer tw-transition-all tw-border tw-border-[var(--wh40k-gold-dark)] tw-text-[var(--wh40k-color-gold)] hover:tw-translate-y-[-1px]"
                            style="background: linear-gradient(135deg,rgba(212,175,55,0.2) 0%,rgba(212,175,55,0.1) 100%)"
                            data-action="addProperty"
                            data-property=""
                            data-list="removed">
                        <i class="fas fa-minus"></i>
                        Remove
                    </button>
                </div>
            {{/if}}
        </div>
    </div>
</div>

<script>
// Wire up the select dropdowns to the add buttons
document.querySelectorAll('.wh40k-property-select').forEach(select => {
    select.addEventListener('change', (e) => {
        const list = e.target.dataset.addList;
        const button = e.target.parentElement.querySelector(`[data-list="${list}"]`);
        if (button) {
            button.dataset.property = e.target.value;
            button.disabled = !e.target.value;
        }
    });
});
</script>

<script>
// Wire up the select dropdowns to the add buttons
document.querySelectorAll('.wh40k-property-select').forEach(select => {
    select.addEventListener('change', (e) => {
        const list = e.target.dataset.addList;
        const button = e.target.parentElement.querySelector(`[data-list="${list}"]`);
        if (button) {
            button.dataset.property = e.target.value;
            button.disabled = !e.target.value;
        }
    });
});
</script>
