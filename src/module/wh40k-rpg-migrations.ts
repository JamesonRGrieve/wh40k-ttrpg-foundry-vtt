// All migrations commented out — world data is edited directly via LevelDB scripts.

export async function checkAndMigrateWorld(): Promise<void> {
    // All migration steps disabled. See git history for v2 (img path) and v3 (inception date).
    await Promise.resolve();
}
